generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Modelos serao adicionados quando a POC definir as entidades.
// Campos PostGIS podem ser representados futuramente com Unsupported("geometry").
